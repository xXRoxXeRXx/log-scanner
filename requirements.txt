# Nextcloud Log Analyzer Requirements

# Core dependencies
# tkinter is included with Python - no pip install needed
# If tkinter is missing on Linux, install via system package manager:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   Fedora: sudo dnf install python3-tkinter
#   macOS: Should be included with Python from python.org

# Optional but recommended
tkinterdnd2>=0.3.0  # For drag & drop support
openpyxl>=3.0.0     # For Excel export
tkcalendar>=1.6.0   # For visual datepicker in time filters

# Development dependencies
# (uncomment for development)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=6.0.0
# mypy>=1.0.0
