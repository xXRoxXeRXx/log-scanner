# Desktop Build Requirements
# Diese Datei enthält alle Dependencies für PyInstaller-Build

# Core Dependencies (aus requirements.txt)
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.17
aiofiles==24.1.0
pydantic==2.10.3
pydantic-core==2.27.1

# PyInstaller für Build
pyinstaller==6.11.1

# Optional: UPX für bessere Komprimierung
# upx - manuell installieren von https://upx.github.io/
